import { Router } from 'express';

const router = Router();

// TODO: Implement expert routes
router.get('/', (req, res) => res.json({ message: 'Expert routes - Coming soon' }));

export default router;
